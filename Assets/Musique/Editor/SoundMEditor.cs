using System;
using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

[CustomEditor((typeof(Sound)))]
public class SoundMEditor : Editor
{
     /*private SerializedProperty
          name, clip, audioMixerGroup, useRandomVolume, volume, volumeRandom, useRandomPitch,
          pitch, pitchRandom, playOnAwake, loop;

     private void OnEnable()
     {
         name = serializedObject.FindProperty("name");
         clip = serializedObject.FindProperty("clip");
         audioMixerGroup = serializedObject.FindProperty("audioMixerGroup");
         useRandomVolume = serializedObject.FindProperty("useRandomVolume");
         volume = serializedObject.FindProperty("volume");
         volumeRandom = serializedObject.FindProperty("volumeRandom");
         useRandomPitch = serializedObject.FindProperty("useRandomPitch");
         pitch = serializedObject.FindProperty("pitch");
         pitchRandom = serializedObject.FindProperty("pitchRandom");
         playOnAwake = serializedObject.FindProperty("playOnAwake");
         loop = serializedObject.FindProperty("loop");
     }

     public override void OnInspectorGUI()
     {
         
     }*/
}
